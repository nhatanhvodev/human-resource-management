spring:
  datasource:
    url: jdbc:postgresql://localhost:15432/hrms
    driver-class-name: org.postgresql.Driver
    username: hrms
    password: hrms
  data:
    redis:
      host: localhost
      port: 6379
  rabbitmq:
    host: localhost
    port: 5672
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
